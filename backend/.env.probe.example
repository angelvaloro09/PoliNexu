# Copiar a `.env.probe` (en .gitignore). Sólo lo usa `scripts/probe_session.py`.
# Deliberadamente separado de `.env`: ese lo parsea `core.config.Settings` y una
# variable desconocida ahí puede terminar impresa con su valor en un traceback.
PROBE_SCHOOL=upiicsa
PROBE_BOLETA=
PROBE_PASSWORD=
