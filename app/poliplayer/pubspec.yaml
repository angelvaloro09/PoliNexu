name: poliplayer
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  flutter_bloc: ^9.1.1
  equatable: ^2.1.0
  drift: ^2.34.3
  sqlite3_flutter_libs: ^0.6.0+eol
  dio: ^5.11.0
  go_router: ^17.5.0
  get_it: ^9.2.1
  table_calendar: ^3.2.1
  flutter_local_notifications: ^22.3.0
  material_symbols_icons: ^4.2960.0
  path_provider: ^2.1.6
  path: ^1.9.1
  intl: ^0.20.3
  timezone: ^0.11.1
  flutter_timezone: ^5.1.0
  flutter_secure_storage: ^10.3.1
  flutter_localizations:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.34.5
  build_runner: ^2.15.1
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

# Ícono de launcher. Regenerar con:
#   dart run flutter_launcher_icons
# Las imágenes las produce `tool/render_icon.py` a partir de la misma geometría
# que `NexusMark`, para que el ícono y la marca en pantalla no se separen.
flutter_launcher_icons:
  android: true
  ios: false
  image_path: assets/branding/icon.png
  # Ícono adaptativo: el launcher recorta el primer plano según la forma del
  # sistema, por eso esa capa lleva más margen que el ícono legacy.
  adaptive_icon_background: "#F8F9FA"
  adaptive_icon_foreground: assets/branding/icon_foreground.png
  # Íconos temáticos de Android 13+: el sistema los recolorea con la paleta
  # del wallpaper, así que la fuente debe ser una silueta plana.
  adaptive_icon_monochrome: assets/branding/icon_monochrome.png

# Splash nativo. Regenerar con:
#   dart run flutter_native_splash:create
# Antes era blanco sólido en ambos temas: al arrancar en modo oscuro daba un
# destello blanco antes de que Flutter pintara el primer frame.
flutter_native_splash:
  color: "#F8F9FA"
  color_dark: "#131314"
  image: assets/branding/splash_light.png
  image_dark: assets/branding/splash_dark.png
  android_12:
    color: "#F8F9FA"
    color_dark: "#131314"
    image: assets/branding/splash_light.png
    image_dark: assets/branding/splash_dark.png

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  # Plus Jakarta Sans (OFL) empaquetada. Antes se descargaba en tiempo de
  # ejecución vía `google_fonts`: sin conexión la app entera caía a Roboto y el
  # sistema tipográfico desaparecía.
  assets:
    - assets/branding/

  fonts:
    - family: PlusJakartaSans
      fonts:
        - asset: assets/fonts/PlusJakartaSans-Regular.ttf
          weight: 400
        - asset: assets/fonts/PlusJakartaSans-Medium.ttf
          weight: 500
        - asset: assets/fonts/PlusJakartaSans-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/PlusJakartaSans-Bold.ttf
          weight: 700
